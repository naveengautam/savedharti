# savedharti

A vibrant, dynamic campaign website concept for environment-friendly initiatives.

## Preview locally

Open `index.html` directly in a browser, or run:

```bash
python3 -m http.server 4173
```

Then visit `http://localhost:4173`.
